<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= docTitle %></title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />

</head>

<% var activeShop,activeAddProduct,breadcrumb; %>

<body>
    <header class="main-header">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="/">NodeJs商城</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item <% if(activeShop){%>active<%}%>">
                        <a class="nav-link" href="/">商城</a>
                    </li>
                    <li class="nav-item <% if(activeAddProduct){%>active<%}%>">
                        <a class="nav-link" href="/admin/add-product">添加产品</a>
                    </li>

                </ul>
            </div>
        </nav>

    </header>

    <% if(breadcrumb){%>
    <%- include('./breadcrumb.ejs') %>
    <%}%>
    <main class=" container p-5">